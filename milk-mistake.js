
let cart = [

    'flour',
    'milk',
    'milk',
    'eggs',
]

console.log(cart);

let index = cart.lastIndexOf('milk');

console.log('The extra item is at index: ' + index);

// indexOf(Will return the first index where 'milk' is in the array)
// lastIndexOf(Will return the last index where 'milk' is in the array)
